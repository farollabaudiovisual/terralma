---
interface Props { eyebrow?: string; title: string; id?: string; class?: string; }
const { eyebrow, title, id, class: className } = Astro.props;
---
<div class:list={['section-heading', className]}>
  {eyebrow && <p class="eyebrow">{eyebrow}</p>}
  <h2 id={id}>{title}</h2>
  <slot />
</div>
